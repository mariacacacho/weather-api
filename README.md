# Forward Geolocation + Weather App

This is a demo application demonstrating a backendless application connecting to two APIs through KOR Connect to secure the API keys. 

## Usage

Run with `yarn start`

notes
1. remove styles in app.css